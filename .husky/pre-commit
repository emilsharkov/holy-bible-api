if git diff --cached --name-only | grep -q "^api/"; then
  echo "Running precommit for folder1"
elif git diff --cached --name-only | grep -q "^web/"; then
  echo "Running precommit for folder2"
else
  echo "No specific folder matched. Skipping."
fi